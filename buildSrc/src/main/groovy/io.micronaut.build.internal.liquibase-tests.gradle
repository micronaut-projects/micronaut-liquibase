plugins {
    id "io.micronaut.build.internal.liquibase-base"
    id "java-library"
}
repositories {
    mavenCentral()
}
dependencies {
    testRuntimeOnly(mnTest.junit.platform.suite)
}
tasks.named("test") {
    useJUnitPlatform()
}
