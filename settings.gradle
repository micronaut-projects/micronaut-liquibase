pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
plugins {
    id 'io.micronaut.build.shared.settings' version '5.0.3'
}

rootProject.name = 'liquibase-parent'

include 'liquibase'
