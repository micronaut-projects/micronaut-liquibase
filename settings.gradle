pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
plugins {
    id 'io.micronaut.build.shared.settings' version '6.1.0'
}

rootProject.name = 'liquibase-parent'

include 'liquibase'
include 'liquibase-bom'
